# nick-light-card (fixed lit import)

This version uses Home Assistant's global Lit exports instead of importing from "lit", so it works on systems where "lit" is not exposed as a bare module specifier.

Replace your existing `nick-light-card.js` with this one.
